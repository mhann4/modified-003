self.addEventListener('fetch', function(event) {
  // This service worker does nothing special, it just fetches the requested resource.
  // Caching is not used, so the app will not work offline.
});
